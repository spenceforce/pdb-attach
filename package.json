{
  "name": "pdb-attach",
  "description": "A python debugger that can attach to running processes.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/smitchell556/pdb-attach.git"
  },
  "keywords": [],
  "author": "",
  "private": true,
  "bugs": {
    "url": "https://github.com/smitchell556/pdb-attach/issues"
  },
  "homepage": "https://github.com/smitchell556/pdb-attach#readme",
  "devDependencies": {
    "@semantic-release/exec": "^5.0.0",
    "@semantic-release/git": "^9.0.0",
    "semantic-release": "^17.3.0"
  },
  "release": {
    "branches": [
      "+([0-9])?(.{+([0-9]),x}).x",
      "master",
      "main",
      "next",
      "next-major",
      {
        "name": "beta",
        "prerelease": true
      },
      {
        "name": "alpha",
        "prerelease": true
      }
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      [
        "@semantic-release/git",
        {
          "assets": [
            "CHANGELOG.md",
            "package.json",
            "package-lock.json",
          ],
          "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
        }
      ],
      [
        "@semantic-release/exec",
        {
          "prepareCmd": "export SKIP_GENERATE_RENO=1 && export SKIP_WRITE_GIT_CHANGELOG=1 && export SKIP_GIT_SDIST=1 && export PBR_VERSION=${nextRelease.version} && python setup.py sdist bdist_wheel",
          "publishCmd": "twine upload dist/*"
        }
      ]
    ]
  }
}
